using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp2.animal
{
    class Birds : Animal
    {
        public Birds(string name, int age, string type, string capability) : base(name, age, type, capability)
        {
        }
    }
}